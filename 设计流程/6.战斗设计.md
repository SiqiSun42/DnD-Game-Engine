在回合中添加标签：inCombat，用于标记是否处于战斗状态; Participants, 用于记录参与人数

0.使用py文件自动读取inCombat tag, 判断是否在战斗中
如果True，则自动进入战斗回合；
如果False，进入非战斗判别；

1.非战斗判别
- 如果不在战斗中但触发战斗，输出True，并修改标记为战斗状态。
输出：{True}（没战斗但触发战斗）; {False}（没战斗也没触发战斗）；

2.{False}进入分支：正常对话提示词

3.{True}进入分支：战斗前准备提示词
- 读取参与战斗的人数，包括玩家、盟友和敌人。输出数字，如：6，写入Participants值（非战斗时默认为-1）。
- 根据人数生成先攻骰子数，决定攻击顺序。格式如下：
    d20:{1,2,3,4,5,6}
- 根据骰子数和修正计算攻击顺序，输出玩家位置，如：4。如果玩家位置是1，则跳过以下流程。
- 查看规则书得知，武器鉴定一共有以下类型的骰子：d4,d6,d8,d10,d12,其中只有d6会出现2d6的情况。战斗中的骰子数均为一次性生成，由DM决定是否使用，使用哪些值。格式如下
    d20:{1,2,3,4,5,6}
    d4:{1,2,3,4,1,2}
    d6(1):{1,2,3,4,5,6}
    d6(2):{1,2,3,4,5,6}
    d8:{1,2,3,4,5,6}
    d10:{1,2,3,4,5,6}
    d12:{1,2,3,4,5,6}
- 生成第0回的游戏流程，从第一顺位到玩家之前的所有战斗

4.正常游戏回合：游戏回合提示词
- 玩家输入操作后，系统自动读取Participants值，触发dice-combat.py为每个参与角色生成7组骰子值（d20, d4, 2d6, d8, d10, d12）
- 根据玩家操作，使用第一组数据生成玩家操作的结果。
- 自动根据当前局势生成每个NPC的动作，然后使用数据计算战斗鉴定的结果。
- 在以下情况可以“跳过”角色：HP清零；特殊状态，如石化；敌人已经全部被打倒（本回合会结束游戏，但此时尚未结束）；

5.在游戏回合结束后，进行判断游戏是否结束。如果结束，输出“战斗结束”，并修改inCombat标签为False，Participants标签为-1.


prompt的话，需要写这样几个： 
1.在非战斗情况下，是否触发战斗判别。返回：True/False，如果为True修改inCombat tag 
2.战斗开始前，判断参与战斗人数。返回：N（一个整数）并修改participant参数 
3.生成先攻顺序。返回：先攻顺序 
4.判断玩家位置。返回：K（一个整数） 
5.第0回，如果K不是1的话。返回：在玩家前的NPC自动战斗回合，以及玩家提示（你要做什么？） 
6.正常战斗回。返回：从玩家到玩家前一个NPC的所有战斗回合，以及玩家提示。 7.判断是否战斗结束。在战斗开始后附着于每一个回合最后。
8.（调试专用）如果非战斗且未触发战斗，则输出“未触发战斗”，然后正常生成文本。
1（如果是True）2345是连续进行的，如果K不等于1，会有三次连续的DM输出（触发战斗、先攻顺序和可能的第0回）。在第0回以后玩家才能再次输入。 7会附着于5和6之后（防止敌人太弱，在第0回玩家没介入就结束战斗。理论上有可能发生）

py文件： 1.（已完成）骰子生成器，输入N，生成N*14个骰子值。标注第一组和第二组数据，外加每个骰子（如d20）
 2.读取inComabt tag的py文件。直接决定进入1还是8的提示词。 
 3.（不确定）修改参数是否应该用py文件而不是依靠prompt进行？如果是的话，1、2之后需要添加修改参数的py文件。只修改inCombat和Participant。但似乎DS可以进行修改参数操作。这个再说。 
 4.（同不确定）用py文件判断玩家是否为顺位1.同样，理论上DS自己就可以判断并且输出K了，但是用py文件可能更标准化一点？接受K并输出True/False，然后再在流程内部进入分支。 （以上两个或许也可以直接写入流程py文件） 
 5.Combat_test.py。流程文件，参考check_test的格式。必须生成。
所以如果234可以合并入流程的话，真正需要新写的只有dice（已经完成）和Combat_test.py，然后在Combat_test.py里面调用提示词。其他函数只用修改接口即可，不需要重新生成任何文件。