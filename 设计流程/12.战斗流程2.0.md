1. 判断是否进入战斗状态 - False或者其他任何字段 则进入正常流程推进环节
- 判断标准为，是否触发battle中预制的战斗。如果触发，输出battle_id，否则输出False。
- python自动检查输出是否为合法的battle_id，如果是，则自动修改battle_data.json的battle_id字段为当前战斗id。
- 在终端print该字段检查。

2. 输出battle_id则进入战斗状态。